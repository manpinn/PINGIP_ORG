<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - PINGIP.ORG</title>
    <script type="importmap"></script>
    <link rel="stylesheet" href="~/lib/twitter-bootstrap/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    @* <link rel="stylesheet" href="~/PINGIP_ORG.styles.css" asp-append-version="true" /> *@
    <link rel="stylesheet" href="~/css/Layout.css" asp-append-version="true" />
    @* <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"> *@
    <link rel="stylesheet" href="~/lib/bootstrap-icons/font/bootstrap-icons.min.css">
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7108910261273960"
     crossorigin="anonymous"></script>
</head>
<body>
    <div class="navbar-ml ml-navbar-grid-container bg-primary">
        <div class="navbar-ml-left">
            <i id="navbar-ml-menu" class="bi-list navbar-ml-menu"></i>
        </div>
        <div class="navbar-ml-center navbar-ml-title">PINGIP.org</div>
        @* <div class="navbar-ml-right">
            <form class="d-flex" role="search">
                <input class="form-control me-2 navbar-ml-right-search" type="search" placeholder="" aria-label="Search">
                <button class="btn btn-outline-light" type="submit">Search</button>
            </form>
        </div> *@
        <div class="navbar-ml-right">
            <a href="https://github.com/energy3088/PINGIP_ORG" target="_blank" class="github-link">
                <img src="https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png" alt="GitHub" width="30" height="30" />
            </a>
        </div>

    </div>

    <div class="ml-sidebar-content">
        <div id="ml-sidebar" class="ml-sidebar">
            <div class="ml-sidebar-scroll">
                <!-- content of the sidebar -->
                <p style="color:lightgray; margin-left:10px; margin-top:10px;">Tools</p>
                <p>@Html.ActionLink("Ping IP", "Index", "Home", null, new { @class = "btn btn-light btn-lg ml-sidebar-button" })</p>
                <p>@Html.ActionLink("Port Check", "Index", "PortCheck", null, new { @class = "btn btn-light btn-lg ml-sidebar-button" })</p>
                <p>@Html.ActionLink("TraceRoute", "Index", "TraceRoute", null, new { @class = "btn btn-light btn-lg ml-sidebar-button" })</p>
                <p>@Html.ActionLink("My IP", "Index", "MyIP", null, new { @class = "btn btn-light btn-lg ml-sidebar-button" })</p>
                <p style="height:2px; background-color:lightgray; margin-left:10px; margin-right:10px;"></p>
                <p style="color:lightgray; margin-left:10px;">Information</p>
                <p>@Html.ActionLink("Legal", "Legal", "Home", null, new { @class = "btn btn-light btn-lg ml-sidebar-button" })</p>
            </div>
        </div>
        <div class="container-ml" style="margin-left:10px; margin-right:10px;">
            <div class="ml-main">
                <main role="main" class="pb-3">
                    @RenderBody()
                </main>
                <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top" style="padding-bottom: 4rem !important;">
                    <div class="col-md-5 d-flex align-items-center">
                        <span class="mb-3 mb-md-0 text-primary" style="margin-right:20px;">© @DateTime.Now.Year Regresik d.o.o.</span>
                        <span class="mb-3 mb-md-0">
                            @Html.ActionLink("Legal", "Legal", "Home", null, new { @class = "icon-link icon-link-hover bi-clipboard" })
                        </span>
                    </div>
                </footer>
            </div>
        </div>
    </div>

    @* <div class="modal fade" id="advertModal" tabindex="-1" aria-labelledby="adModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title" id="adModalLabel">Our newest project</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Zatvori"></button>
                </div>
                <div class="modal-body">
                   
    <div class="card-body" style="text-align: center;">
        <h5 class="card-title">FlightCraft</h5>
        <p class="card-text">
            <span>
                                Experience Minecraft Survival like never before.
                                Every player on our server enjoys full Fly‑Mode, unlocking a completely new way to explore, build, and survive.
                                Access is available only after payment, ensuring a clean and protected community.
                                If you want to try it out first, a one‑day trial is waiting for you — no strings attached.
        </span>
    </p>
    </div>
    <div style="padding:5px;">
        <img src="~/pic/flightcraft_1.jpg" class="img-fluid img-thumbnail rounded mx-auto d-block" alt="Pic 4">
    </div>
    <div class="text-center" style="padding-top: 10px;">
        <a href="https://gamesonweb.net" class="btn btn-primary bi bi-play mb-3" target="_blank">
            Go to
        </a>
    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Popup --> *@
<div id="consent-popup" class="popup hidden">
  <div class="popup-content">
    <h3>Cookie Settings</h3>
    <p>We use Google Analytics. Please choose your preference.</p>

    <button id="consent-accept">Accept</button>
    <button id="consent-deny">Decline</button>
  </div>
</div>

<style>
  .popup {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.6);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 99999;
  }
  .popup.hidden { display: none; }

  .popup-content {
    background: #fff;
    padding: 20px;
    border-radius: 8px;
    width: 320px;
    text-align: center;
    box-shadow: 0 0 20px rgba(0,0,0,0.3);
  }

  button {
    margin: 10px;
    padding: 10px 15px;
    border: none;
    border-radius: 6px;
    cursor: pointer;
  }

  #consent-accept { background: #4caf50; color: #fff; }
  #consent-deny { background: #f44336; color: #fff; }
</style>

<script>
  // Check stored consent
  const consent = localStorage.getItem("consent");
  const popup = document.getElementById("consent-popup");

  // Show popup only if no decision exists
  if (!consent) {
    popup.classList.remove("hidden");
  }

  // Accept
  document.getElementById("consent-accept").onclick = () => {
    localStorage.setItem("consent", "accepted");
    popup.classList.add("hidden");
    loadAnalytics();
  };

  // Deny
  document.getElementById("consent-deny").onclick = () => {
    localStorage.setItem("consent", "denied");
    popup.classList.add("hidden");
  };

  // Load GA only if accepted
  if (consent === "accepted") {
    loadAnalytics();
  }

  // Google Analytics loader
  function loadAnalytics() {
    const s = document.createElement("script");
                s.src = "https://www.googletagmanager.com/gtag/js?id=G-GPR48YBGC9";
    document.head.appendChild(s);

    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag("js", new Date());
                gtag("config", "G-GPR48YBGC9");
  }
</script>

    <script>
        const mediaQuery = window.matchMedia('(max-width: 1000px)');

        const main = document.querySelector('.container-ml');

        const sidebar = document.querySelector('.ml-sidebar');

        function showSidebar() {
            sidebar.style.width = '200px';
            sidebar.style.opacity = '1';
            sidebar.classList.add('active');

            main.style.marginLeft = '210px';
            main.classList.add('active');
        }

        function hideSidebar() {
            sidebar.style.width = '0px';
            sidebar.style.opacity = '0';
            sidebar.classList.remove('active');

            main.style.marginLeft = '10px';
            main.classList.remove('active');
        }

        function handleMediaQueryChange(event) {
            if (event.matches) {
                // Media query matches (screen width is 1000px or less)
                sidebar.style.width = '0px';
                sidebar.style.opacity = '0';
                sidebar.style.transition = 'width 0.3s ease-in-out, opacity 0.3s ease-in-out';
                sidebar.classList.remove('active');

                main.style.marginLeft = '10px';
                main.style.transition = 'margin-left ease 0.3s';
                main.classList.remove('active');
            } else {
                // Media query does not match (screen width is greater than 1000px)
                sidebar.style.width = '';
                sidebar.style.opacity = '';
                sidebar.style.transition = '';
                sidebar.classList.add('active');

                main.style.marginLeft = '210px';
                main.classList.remove('active');
            }
        }

        mediaQuery.addListener(handleMediaQueryChange);

        handleMediaQueryChange(mediaQuery);

        document.getElementById('navbar-ml-menu').addEventListener('click', function () {
            if (sidebar.classList.contains('active')) {
                hideSidebar();
            } else {
                showSidebar();
            }
        });

        document.getElementsByClassName('container-ml')[0].addEventListener('click', function () {
            if (window.innerWidth < 1000 && sidebar.classList.contains('active')) {
                hideSidebar();
            }
        });

            // document.addEventListener("DOMContentLoaded", function() {

            //     if (!sessionStorage.getItem("advertModalShown")) {

            //         const modalEl = document.getElementById('advertModal');
            //         const modal = new bootstrap.Modal(modalEl);

            //         setTimeout(() => {
            //             modal.show();

            //             sessionStorage.setItem("advertModalShown", "true");

            //         }, 300000);
            //     }
            // });




    </script>
    <script src="~/lib/twitter-bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
